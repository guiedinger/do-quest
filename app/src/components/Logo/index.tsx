import { Typography } from "@mui/material";

export const Logo = () => {
  return (
    <Typography
      variant="h6"
      component="div"
      sx={{ flexGrow: 1, fontFamily: "Retrips" }}
    >
      DoQuest
    </Typography>
  );
};
